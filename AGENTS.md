# 仓库说明

> 该 AGENTS.md 是本仓库的 Skill 开发规则说明，适用于整个仓库的 Skill，规则冲突时，以更具体的规则为准。

本仓库存放个人使用的 [Agent Skills](https://agentskills.io/home)，Skill 应记录 Agent 默认未必知道的行为：个人工作流、反复纠正过的问题、领域特定知识、偏好的边界，以及实践中观察到的失败模式，它们用于扩展 Agent 的能力边界，设计为被 Agent 而不是人类用户直接使用。
这些 Skill 不是中立的通用模板；它们记录了用户对 Agent 如何协作、编辑、审查和验证工作的明确个人偏好。

已有的 Skill 及其职责说明统一维护在 [README.md](README.md) 及它的其它语言版本中。

## 代码规则

- 项目中的脚本语言使用 Python，统一使用 uv 作为包管理工具。
- 使用 Ruff 和 Pyright 做静态检查和格式化检查，Pytest 作为测试框架。
- 使用 .bat 脚本调用 uv 完成 Windows 下的虚拟环境配置。

## Skill 文档规则

> 本章节内容是针对单个 Skill 文档的规则，文档路径均为相对于特定 Skill 根目录的路径

- 保证文档单一职责和内容的单源性，同一内容优先使用引用而不是在多个文档重复维护。
- 当内容较长、会被独立复用，或属于架构、错误处理等稳定主题时，拆分为 reference 下的引用文档，短规则和单次说明允许保留在所属文档中。

### 语言规则

- Skill.md 正文、references 文档使用英文，但是允许示例及模板使用其他语言。
- README.md、AGENTS.md 使用中文。

### SKILL.md

- Skill 根目录的**必需**文档，面向使用 Skill 的 Agent，必须具有至少包含 name，description 两个字段的 YAML 头。
- 正文使用结构化组织方式，至少包含 Skill 的适用范围、核心规则、脚本运行环境配置（若有脚本）等 Skill 正常使用的必需信息。
- 指令简洁明确，并优先使用表格和“场景-正确行为-禁止行为”的描述结构。
- 优先写具体情景下的具体行为规则，不写冗长的背景或通用知识说明。
- 仅在 Skill.md 中 YAML 头的 `description` 中，把中文以对应英文术语旁边的括注的形式标注，例如 `Markdown formatting(格式调整)`。

### README.md

- Skill 根目录的**可选**文档，面向人类，包含 Skill 的概述、使用边界、运行环境、隐私安全和进一步阅读资料等用户关心的问题。
- 不要在其中详细描述 Skill 的触发条件、执行流程、输入输出和能力边界等技术或实现细节，这些内容都有专门的文档记录，README 中只需要引用这些文档。

### AGENTS.md

- 根目录的**可选**文档，面向开发者，包含 Skill 代码的常用命令、规则约束和风格偏好。

### references/ARCHITECTURE.md

- **可选**的代码架构说明文档，在此说明 Skill 的架构设计。
- 应当说明各逻辑模块的职责和边界、Skill 执行的控制流程、数据流向。
- 不应当锁定代码内部的实现细节。

### references/ERROR-HANDLING.md

- **可选**的常见错误处理方式说明文档，在此说明 Skill 使用中可能遇到的问题及其一般处理方法。

### assets

- **可选**的文件夹，统一存放 Skill 需要的模板、图片、字体、指令等资源。


## 编辑规则

- 已实现的 Skill 行为以仓库中的代码为第一可信状态源，而不是记忆中的状态、已安装 Skill 的状态或文档的描述。
- 删除或废弃功能时，同时删除对应说明、示例和引用。
- 保留无关的本地修改和未跟踪文件，保留代码中的正确注释。
- 禁止仅追加新规则来覆盖旧规则，存在冲突时，应修改或删除旧规则，使当前有效规则唯一且明确。
- 修改一个 Skill 时，除非用户明确要求，否则不要修改其他 Skill。
- 完成 Skill 的触发条件、输入输出、配置格式、目录结构、命令或执行流程等用户可见行为修改后，必须检查并同步相关文档。
- 代码或脚本修改后，运行 Skill 规定的测试和代码规范检查。
- 修改文档后，检查链接、路径、YAML 头和示例是否有效。

## 提交信息

编写提交信息时，遵循 [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/) 规范。
